/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
}

module.exports = {
  env: {
    PUBLIC_URL: "https://bishrel.github.io/bishrels.github.io/",
    assetPrefix: './'
  }
};
